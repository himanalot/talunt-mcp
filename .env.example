# Copy to .env and fill in your real keys. Loaded automatically by academic_cli.py.
#
# Fireworks AI — for `acad ai-render-messages` (LLM-based per-candidate personalization)
# Sign up at https://fireworks.ai/ , account.fireworks.ai → API keys
FIREWORKS_API_KEY=fw_xxx
